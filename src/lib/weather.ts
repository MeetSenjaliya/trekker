// Active seasonal theme. Flip this one value to switch the whole app:
//   'rain' — monsoon/rainy season    'snow' — winter    'none' — disable
export type Weather = 'rain' | 'snow' | 'none';

export const WEATHER: Weather = 'rain';
